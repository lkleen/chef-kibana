#! /bin/sh
# /etc/init.d/kibana
#
# created by chef
#

case "$1" in
    start)
        /usr/local/rvm/bin/ruby-1.9.3-p327 -C/opt/kibana kibana-service.rb start
        exit $?
        ;;
    stop)
        /usr/local/rvm/bin/ruby-1.9.3-p327 -C/opt/kibana kibana-service.rb stop
        exit $?
        ;;
    restart)
        /usr/local/rvm/bin/ruby-1.9.3-p327 -C/opt/kibana kibana-service.rb restart
        exit $?
        ;;
    status)
        /usr/local/rvm/bin/ruby-1.9.3-p327 -C/opt/kibana kibana-service.rb status
        exit $?
        ;;
    *)
        echo "Usage: /etc/init.d/kibana {start|stop|restart|status}"
        exit 1
        ;;
esac

exit 0